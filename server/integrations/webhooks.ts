// server/integrations/webhooks.ts
// Os webhooks agora são tratados dentro de api/trpc.ts.
// Este arquivo é mantido apenas para compatibilidade de importação.
export function registerWebhooks(_app: unknown) {
  // no-op: webhook routes are handled in api/trpc.ts
}
